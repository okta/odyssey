# `@okta/unified-ui-shell`

The Unified UI Shell that should be consumed by other teams. Wraps the Odyssey function with appropriate framework for doing API calls related to App Switcher and injecting those into the passed props.
