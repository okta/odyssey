# @okta/font

A centralized package that holds all used fonts at Okta and CSS for importing them.

## How to use

- Fonts are located in `@okta/font/fonts`.
- Styles are in `@okta/font/css` as `fonts.css`.
