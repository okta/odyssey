## Generating Okta Test Language files

The `*_ok_PL.properties` and `*_ok_SK.properties` will automatically be generated pre-commit if there are changes to the
main `.properties` file.

To manually generate them, run the following command:

```bash
yarn generate:i18n:pseudoLocaleProperties
```
