import {
  Canvas,
  Meta,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
} from "@storybook/addon-docs";
import { hrefTo } from "@storybook/addon-links";
import LinkTo from "@storybook/addon-links/react";
import { Story } from "@storybook/blocks";

import { Link } from "@okta/odyssey-react-mui";
import * as MenuButtonStories from "./MenuButton.stories";

<Meta of={MenuButtonStories} />
<Title of={MenuButtonStories} />

Children of `MenuButton` should be one or more `MenuItem` components.

[see `MenuItem` docs](/docs/mui-components-menuitem--docs)

<Subtitle of={MenuButtonStories} />
<Description of={MenuButtonStories} />
<Primary of={MenuButtonStories} />
<Controls />
<Stories />
