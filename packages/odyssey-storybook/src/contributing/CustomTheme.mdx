import { Canvas, Meta, Story } from "@storybook/blocks";
import * as CustomThemeStories from "./CustomTheme.stories";

<Meta of={CustomThemeStories} />

# Custom Theme

A way to use a custom theme to override the Odyssey default theme.

## Behavior

Visually, the MUI components under `OdysseyThemeProvider` with a custom theme would consume that theme.

### Button Primary

<Canvas of={CustomThemeStories.ButtonStory} />

### Input Default

<Canvas of={CustomThemeStories.TextFieldStory} />

### Radio Default

<Canvas of={CustomThemeStories.RadioGroupStory} />
