import { Meta } from "@storybook/blocks";
import { TokenTables } from "../../.storybook/components";
import * as TokensStories from "./Tokens.stories";

<Meta of={TokensStories} />

# Design Tokens

Design Tokens abstract Base elements into names that are commonly understood. They are then used together to build components.

<TokenTables />
